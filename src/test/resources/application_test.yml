#spring:
 # datasource:
  #  driver-class-name: org.h2.Driver
   #   url: jdbc:h2:mem:test
    #  username: sa
     # password:

  #h2:
   # console:
    #  enabled: true
     # path: /h2-console

  #jpa:
   # database-platform: org.hibernate.dialect.H2Dialect
    #database: h2
    #hibernate:
     # ddl-auto: create
    #show-sql: true #JPA가 생성한 SQL문을 보여주기
    #format-sql: true # SQL문을 좀 더 보기 편하게 보여준다.
    #generate-ddl: true # ddl-auto 속성을 사용할지 말지를 결정하는 옵션. JPA에 의한 자동 초기화 기능을 사용하기 위해 사용